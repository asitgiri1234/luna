# store/

Global client state lives here (e.g. Zustand slices) once the app grows
past what local component state and router state can express.

Intentionally empty in the foundation milestone — do not add state
management until a feature actually needs shared state.
